--- a/drivers/gpu/drm/amd/pm/amdgpu_pm.c
+++ b/drivers/gpu/drm/amd/pm/amdgpu_pm.c
@@ -3515,12 +3515,16 @@ static ssize_t amdgpu_hwmon_set_pwm1_enable(struct device *dev,
        struct amdgpu_device *adev = dev_get_drvdata(dev);
        long value;
        int ret;
-       bool supported;
+       bool supported;

        ret = kstrtol(buf, 10, &value);
        if (ret)
                return ret;

+       /*
+        * Force-allow manual fan control even if FAN_CONTROL feature
+        * is reported disabled in pp_features.
+        */
        if (value == 1) {
-               supported = amdgpu_dpm_is_overdrive_supported(adev) &&
-                           amdgpu_dpm_is_fan_ctrl_supported(adev);
+               supported = true;
                if (!supported)
                        return -EOPNOTSUPP;
                ret = amdgpu_dpm_set_fan_control_mode(adev, 1);

